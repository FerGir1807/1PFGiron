import { EstatusAlumnoDirective } from './estatus-alumno.directive';

describe('EstatusAlumnoDirective', () => {
  it('should create an instance', () => {
    const directive = new EstatusAlumnoDirective();
    expect(directive).toBeTruthy();
  });
});
