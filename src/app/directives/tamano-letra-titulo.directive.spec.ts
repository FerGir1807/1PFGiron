import { TamanoLetraTituloDirective } from './tamano-letra-titulo.directive';

describe('TamanoLetraTituloDirective', () => {
  it('should create an instance', () => {
    const directive = new TamanoLetraTituloDirective();
    expect(directive).toBeTruthy();
  });
});
